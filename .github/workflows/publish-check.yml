name: Publish Guard

on:
  pull_request:
  push:
    branches: [ "main" ]

jobs:
  publish-guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run publish guard
        run: ./publish.sh
